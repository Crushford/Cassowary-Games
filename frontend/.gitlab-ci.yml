image: 'node:20'

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - yarn install --immutable
    - yarn build
  artifacts:
    paths:
      - public

pages:
  stage: deploy
  script:
    - echo 'Pages deployment job'
  artifacts:
    paths:
      - public
  only:
    - main
