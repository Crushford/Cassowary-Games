image: 'node:20'

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - cd frontend
    - yarn install --immutable
    - yarn build
  artifacts:
    paths:
      - frontend/dist

pages:
  stage: deploy
  script:
    - echo 'Pages deployment job'
  artifacts:
    paths:
      - frontend/dist
  only:
    - main
